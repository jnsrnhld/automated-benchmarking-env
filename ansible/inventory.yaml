# all cloud machines
remote:
  hosts:
    leaderNode:
      ansible_host: 34.32.36.52
    workerNode1:
      ansible_host: 34.32.49.8
    workerNode2:
      ansible_host: 34.32.43.67

# 1 host coordinating k8s via kubectl/helm, running bridge service, scaling services, database, etc.
leader:
  hosts:
    leaderNode:

# all machines that contain the batch processing framework and/or hdfs
follower:
  hosts:
    leaderNode:
    workerNode1:
    workerNode2:
