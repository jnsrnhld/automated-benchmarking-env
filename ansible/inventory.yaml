# all cloud machines
remote:
  hosts:
    leaderNode:
      ansible_host: 34.32.36.193
    workerNode1:
      ansible_host: 34.32.5.70
#    workerNode2:
#      ansible_host: 34.32.36.193

# 1 host coordinating k8s via kubectl/helm, running bridge service, scaling services, database, etc.
leader:
  hosts:
    leaderNode:

# all machines that contain the batch processing framework and/or hdfs
follower:
  hosts:
    workerNode1:
#    workerNode2:
